import React from "react";

const CreateTeam = () => {
  return (
    <>
      <h1>Create Team</h1>
      <p>
        This will be a form where you can create a team (and maybe join a team?
        TBD, depends on how invested we want to get, but shouldn't be too hard)
      </p>
    </>
  );
};

export default CreateTeam;
